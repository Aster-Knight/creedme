import { EloChangePipe } from './elo-change-pipe';

describe('EloChangePipe', () => {
  it('create an instance', () => {
    const pipe = new EloChangePipe();
    expect(pipe).toBeTruthy();
  });
});
