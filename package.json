{
  "name": "creedme-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build:angular": "cd creedme-angular && npm install && ng build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@google/generative-ai": "^0.1.3",
    "firebase-admin": "^12.0.0",
    "node-fetch": "^2.7.0"
  }
}