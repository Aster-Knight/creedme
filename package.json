{
  "name": "creedme-monorepo",
  "version": "1.0.0",
"scripts": {
  "build": "npm install && cd creedme-angular && npm install && ng build",
  "test": "ng test"
},
  "dependencies": {
    "@google/generative-ai": "^0.1.3",
    "firebase-admin": "^12.0.0",
    "node-fetch": "^2.7.0"
  }
}